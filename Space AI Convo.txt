==================================================================
SPACE STATION COMMANDER - DEPLOYMENT BLUEPRINT & HISTORY
==================================================================
[Context] React Vite Frontend + Node.js (server.ts) Backend. 
[Local Path] C:\Users\hp\Downloads\space-station-commander
[Server IP] 102.133.160.133 (Ubuntu VM hosted on Azure)
[SSH Key Path] C:\Users\hp\Downloads\SpaceStationServer.pem
[Process Runner] PM2 (Process name: "space-universe")

AUTOMATED ACTIONS PIPELINES CONFIGURED:
1. Web/Server: GitHub Actions ("Automatic Azure Deployment") hooks 
   on 'git push origin main'. It auto-logs into Azure via SSH, runs 
   the compiler, and restarts PM2.
2. Mobile APK: GitHub Actions ("Build APK") builds the Android assets 
   in the cloud. Accessible via the browser 'Actions' tab under 'Artifacts'.

==================================================================
OPERATIONAL COMMAND CHAT SHEET
==================================================================
1. ONE-LINE QUICK LOCAL PUSH (Run from any CMD root):
cd C:\Users\hp\Downloads\space-station-commander && git add . && set /p msg="Enter Update Name: " && git commit -m "%msg%" && git push -f origin main

2. LOW-MEMORY DEEP REBUILD & WIPE (Fixes server RAM freezes):
ssh -i C:\Users\hp\Downloads\SpaceStationServer.pem azureuser@102.133.160.133 "cd SpaceStationGame && pm2 stop space-universe && rm -rf dist && echo '{\"players\":{},\"alliances\":{},\"chatMessages\":[],\"fleets\":[],\"battleReports\":[],\"newsEvents\":[]}' > galaxy_state.json && NODE_OPTIONS=\"--max-old-space-size=1024\" npm run build && pm2 start dist/server.cjs --name \"space-universe\" --update-env && pm2 save"

3. SAFE HARDWARE REBOOT (Zero data loss, auto-reloads game):
ssh -i C:\Users\hp\Downloads\SpaceStationServer.pem azureuser@102.133.160.133 "sudo reboot"

4. MANUAL SERVER PROFILE DATABASE RESET:
ssh -i C:\Users\hp\Downloads\SpaceStationServer.pem azureuser@102.133.160.133 "cd SpaceStationGame && echo '{\"players\":{},\"alliances\":{},\"chatMessages\":[],\"fleets\":[],\"battleReports\":[],\"newsEvents\":[]}' > galaxy_state.json && pm2 restart space-universe"
==================================================================

# CONTEXT RESTORATION NODE: SPACE STATION COMMANDER PROJECT

## 🚀 1. SYSTEM ARCHITECTURE & HOSTING
- App Type: Cross-platform mobile game.
- Frontend Framework: React (TypeScript) wrapped inside a Capacitor 6 container.
- Backend Environment: Node.js (Express) running a custom game server architecture.
- Live Infrastructure VM: Azure Standard B2ats v2 Ubuntu Linux Instance.
- Production Server Public IP: 102.133.160.133 (Port 3000).
- Remote Source Management: GitHub Repository (180-lab/Space-Station).
- Deployment Pipelines: GitHub Actions CI/CD automates backend server updates directly to Azure and compiles production client bundles simultaneously.

## 📱 2. LOCAL DEV PROFILE & PATHS
- Windows Host Local Repository Root Path: "C:\Users\hp\Downloads\space-station-commander"
- Client Mobile ID / Android Package Name: com.galactose.tacticalgame
- Native Firebase Configuration Path: C:\Users\hp\Downloads\space-station-commander\android\app\google-services.json

## 🛰️ 3. FIREBASE & NOTIFICATION CONFIGURATION
- Error History Fixed: Resolved a 20-minute connection drop-off caused by mobile battery-saver sleep loops killing Server-Sent Events (SSE). Upgraded system to native system-level pushes.
- Active Core Core Plugins: @capacitor/local-notifications (v6.1.3) & @capacitor/push-notifications (v6.0.2).
- Notification Architecture: Handles alerts via Firebase Cloud Messaging (FCM). Uses high-priority overrides (`android.priority: 'high'` / `apns-priority: '10'`) combined with a max-importance native channel registration (`id: 'game-alerts'`, `importance: 5`) to guarantee instant delivery bypassing device "Doze Modes".
- GitHub Environment Secret Records Loaded:
  * FIREBASE_PROJECT_ID: space-station-498022
  * FIREBASE_SERVICE_ACCOUNT: Full private service account encryption text string generated via Google Cloud IAM Console.

## 🛡️ 4. HARDENED SECURITY ACTIONS ENFORCED
- Server-Authoritative Logic: Eliminated client-side data saving. Core currencies and actions route directly through secure validation endpoints (e.g., `/api/buy-credits`), strictly limiting modifications to authorized system tiers (1500, 6000, 25000 credits).
- Quantum Anti-Speed-Hack Shield: Express rate-limiting middleware blocks automated click-bots or network duplicate macros by dropping rapid consecutive requests executed within < 150ms with a 429 status.
- Obfuscation Engine Enforced: Modified production bundler configs (`vite.config.ts`). Production compilations (`npm run build`) automatically drop console logs, disable .js.map files, and mangle names/identifiers to single characters (`minifyIdentifiers: true`), blocking reverse engineering attempts via APK unzipping.

## 🍏 5. IOS EXPANSION PROFILE
- Shared Assets: Frontend web code requires zero changes to transition onto iOS platforms.
- Native Platform Dependency: Requires an active $99/year Apple Developer Membership, a macOS environment running Xcode, and executing the platform insertion commands: `npm install @capacitor/ios && npx cap add ios`.

## 🛠️ 6. FREQUENT MAINTENANCE TERMINAL COMMANDS
- To recompile local modifications and sync plugins directly into Android folders:
  cmd> npm run build && npx cap sync android
- To safely retrieve remote updates generated by Google AI Studio without code wiping:
  cmd> git pull origin main --no-rebase
- To force-push a change to GitHub to trigger an automated cloud APK compilation:
  cmd> git add . && git commit -m "Deployment Trigger" && git push origin main
  ###log stream command/ Server logs
ssh -i C:\Users\hp\Downloads\SpaceStationServer.pem azureuser@102.133.160.133 "pm2 logs space-universe --lines 0"

==================================================================
ADDENDUM: PRODUCTION MAINTENANCE & LOW-RAM RAM SAFEGUARDS (2026)
==================================================================

[System State Check - July 2026]
- Production Active State: 23 Registered Live Profiles.
- Database Target File: SpaceStationGame/galaxy_state.json (Crucial Data)
- Server Compilation Bypass Architecture: High-volume module bundles (>2000 modules)
  will exhaust the VM's 2GB RAM buffer during Vite frontend processing. Maintenance 
  and mechanics updates must pass directly through server-side 'esbuild'.

==================================================================
OPERATIONAL COMMAND CHAT SHEET EXTENSIONS
==================================================================

1. ULTRA-FAST BACKEND ENGINE UPDATE (Zero Data Loss / Preserves 23 Players)
   Use this when changing 'server.ts' or game mechanics without affecting the frontend.
   It backs up player profiles, forces a clean git pull, compiles via esbuild in 3 seconds,
   restores user database profiles, and cleanly cycles the engine:

ssh -i C:\Users\hp\Downloads\SpaceStationServer.pem azureuser@102.133.160.133 "cd SpaceStationGame && cp galaxy_state.json /tmp/galaxy_state_backup.json && git reset --hard HEAD && git clean -fd && git pull origin main && cp /tmp/galaxy_state_backup.json galaxy_state.json && mkdir -p dist && npx esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs && pm2 restart space-universe"

2. EMERGENCY KERNEL UNLOCK AND BACKGROUND PURGE
   If a deployment stalls silently due to memory exhaustion, this terminates 
   frozen node processes immediately to unlock the VM memory pool:

ssh -i C:\Users\hp\Downloads\SpaceStationServer.pem azureuser@102.133.160.133 "killall -9 node || true"

3. PRODUCTION SYSTEM HEALTH & LIVE LOG STREAMING
   Tracks connection drops, player profile parsing, and blocks automated network 
   vulnerability scanning bots targeting port 3000:

ssh -i C:\Users\hp\Downloads\SpaceStationServer.pem azureuser@102.133.160.133 "pm2 status space-universe && pm2 logs space-universe --lines 20"

4. LOCAL COMPILATION & ANDROID RUNTIME ASSET SYNCHRONIZATION
   Run from Windows CMD root to align local environment changes with Capacitor bundles:

cd C:\Users\hp\Downloads\space-station-commander && npm run build && npx cap sync android

==================================================================
SECURITY DEFENCE LOG ENTRY
==================================================================
- Bot Activity Logged: Global scanning botnets attempting path-traversal exploits 
  (e.g., trying to read /etc/passwd or /proc/self/environ via custom query parameters).
- Shield Enforcement: All live production processes have been moved away from Vite dev 
  listeners onto production 'esbuild' CommonJS modules (`dist/server.cjs`). Native OS path 
  traversals are fully closed and dropped automatically by Express route handlers.
